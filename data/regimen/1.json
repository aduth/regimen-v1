{
  "week": 2,
  "fields": {
    "bp_test_weight": 255,
    "bp_test_interval": 3,
    "bp_1rm": 238,
    "bp_5rm" : 212,
    "pr_week": 4,
    "set_interval": 12,
    "min_plate_weight": 5
  },
  "program": {
    "name": "Madcow 5x5",
    "description": "Originally created by Bill Starr, the writer of the book “The Strongest Shall Survive” this program is great for adding muscle mass and increasing overall strength and fitness levels.",
    "fields": [
      {
        "type": "test",
        "name": "bp",
        "label": "Bench Press"
      },
      {
        "type": "number",
        "name": "pr_week",
        "label": "Match PRs in Week #",
        "description": "The week in which you want to match your previous 5RMs"
      },
      {
        "type": "number",
        "name": "interval",
        "label": "Set Interval",
        "description": "The size of the steps between your ramped sets"
      }
    ],
    "iteration": {
      "type": "week",
      "label": "Week {{week}}"
    },
    "workouts": [
      {
        "name": "Monday",
        "exercises": [
          {
            "name": "Bench Press",
            "interval_type": "reps",
            "sets": [
              {
                "interval": 5,
                "weight": "Math.round(({{regimen.fields.bp_5rm}} * Math.pow(1/1.025, {{regimen.fields.pr_week}}-1) * Math.pow(1.025, {{regimen.week}} - 1)*(1-({{regimen.fields.set_interval}}*4/100))) / regimen.fields.min_plate_weight) * regimen.fields.min_plate_weight"
              },
              {
                "interval": 5,
                "weight": "Math.round(({{regimen.fields.bp_5rm}} * Math.pow(1/1.025, {{regimen.fields.pr_week}}-1) * Math.pow(1.025, {{regimen.week}} - 1)*(1-({{regimen.fields.set_interval}}*3/100))) / regimen.fields.min_plate_weight) * regimen.fields.min_plate_weight"
              },
              {
                "interval": 5,
                "weight": "Math.round(({{regimen.fields.bp_5rm}} * Math.pow(1/1.025, {{regimen.fields.pr_week}}-1) * Math.pow(1.025, {{regimen.week}} - 1)*(1-({{regimen.fields.set_interval}}*2/100))) / regimen.fields.min_plate_weight) * regimen.fields.min_plate_weight"
              },
              {
                "interval": 5,
                "weight": "Math.round(({{regimen.fields.bp_5rm}} * Math.pow(1/1.025, {{regimen.fields.pr_week}}-1) * Math.pow(1.025, {{regimen.week}} - 1)*(1-({{regimen.fields.set_interval}}/100))) / regimen.fields.min_plate_weight) * regimen.fields.min_plate_weight"
              },
              {
                "interval": 5,
                "weight": "Math.round(({{regimen.fields.bp_5rm}} * Math.pow(1/1.025, {{regimen.fields.pr_week}}-1) * Math.pow(1.025, {{regimen.week}} - 1)) / regimen.fields.min_plate_weight) * regimen.fields.min_plate_weight"
              }
            ],
            "text": "{{set.interval}} reps at {{set.weight}} {{unit}}"
          }
        ]
      }
    ]
  }
}
